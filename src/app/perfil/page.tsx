import { redirect } from "next/navigation";import { auth } from "@/auth";import { prisma } from "@/lib/prisma";import { AccountShell } from "@/components/account-shell";import { ProfileForm } from "@/components/profile-form";
export default async function ProfilePage(){const session=await auth();if(!session?.user.id)redirect("/entrar?callbackUrl=/perfil");const p=await prisma.companyProfile.findUnique({where:{userId:session.user.id}});const profile=p?{fullName:p.fullName,companyName:p.companyName,document:p.document,phone:p.phone,address:p.address,city:p.city,state:p.state}:undefined;return <AccountShell admin={session.user.role==="ADMIN"}><div className="page-title"><div><h1>Perfil da empresa</h1><p>Estes dados são obrigatórios antes da assinatura.</p></div></div><div className="panel-card"><ProfileForm profile={profile}/></div></AccountShell>}
